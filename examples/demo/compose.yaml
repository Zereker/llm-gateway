name: llm-gateway-demo

x-app-build: &app-build
  context: ../..
  dockerfile: examples/demo/Dockerfile
  args:
    GOPROXY: ${GOPROXY:-https://proxy.golang.org,direct}

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_DATABASE: llm_gateway
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --default-authentication-plugin=mysql_native_password
    volumes:
      - demo-mysql:/var/lib/mysql
    healthcheck:
      # Use TCP explicitly: localhost may select MySQL's Unix socket and report
      # ready a moment before other containers can connect over the network.
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "--silent"]
      interval: 3s
      timeout: 3s
      retries: 30
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    volumes:
      - demo-redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  mockupstream:
    build:
      <<: *app-build
      target: mockupstream
    environment:
      MOCK_ADDR: ":9090"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:9090/health"]
      interval: 3s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  gateway:
    build:
      <<: *app-build
      target: gateway
    command: ["-config", "/etc/llm-gateway/gateway.yaml"]
    volumes:
      - ./configs/gateway.yaml:/etc/llm-gateway/gateway.yaml:ro
      - demo-gateway-data:/var/lib/llm-gateway
    ports:
      - "8080:8080"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      mockupstream:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:8080/readyz"]
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 5s
    restart: unless-stopped

  seed:
    build:
      <<: *app-build
      target: seed
    command:
      - -dsn
      - root:@tcp(mysql:3306)/llm_gateway?parseTime=true&charset=utf8mb4
      - -data-key
      - 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
      - -upstream
      - http://mockupstream:9090/v1/chat/completions
      - -name
      - demo-openai-fixture
      - -api-key
      - sk-demo-llm-gateway
      - -model
      - mock-openai-model
    depends_on:
      gateway:
        condition: service_healthy
    restart: "no"

  console:
    build:
      <<: *app-build
      target: console
    command: ["-config", "/etc/llm-gateway/console.yaml"]
    volumes:
      - ./configs/console.yaml:/etc/llm-gateway/console.yaml:ro
    ports:
      - "8081:8081"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      seed:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:8081/healthz"]
      interval: 3s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v3.12.0
    profiles: [observability]
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
    volumes:
      - ../prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ../prometheus/alerts.yaml:/etc/prometheus/rules/alerts.yaml:ro
      - demo-prometheus:/prometheus
    ports:
      - "9091:9090"
    depends_on:
      gateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:9090/-/ready"]
      interval: 3s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  grafana:
    image: grafana/grafana:13.0.2
    profiles: [observability]
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - ../prometheus/grafana/provisioning:/etc/grafana/provisioning:ro
      - ../prometheus/dashboard.json:/var/lib/grafana/dashboards/llm-gateway.json:ro
      - demo-grafana:/var/lib/grafana
    ports:
      - "3000:3000"
    depends_on:
      prometheus:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:3000/api/health"]
      interval: 3s
      timeout: 3s
      retries: 30
      start_period: 5s
    restart: unless-stopped

  benchmark-upstream:
    build:
      <<: *app-build
      target: benchmark-upstream
    profiles: [benchmark]
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://localhost:9092/health"]
      interval: 2s
      timeout: 2s
      retries: 20

  benchmark-seed:
    build:
      <<: *app-build
      target: seed
    profiles: [benchmark]
    command:
      - -dsn
      - root:@tcp(mysql:3306)/llm_gateway?parseTime=true&charset=utf8mb4
      - -data-key
      - 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
      - -upstream
      - http://benchmark-upstream:9092/v1/chat/completions
      - -name
      - benchmark-upstream
      - -api-key
      - sk-demo-llm-gateway
      - -model
      - benchmark-model
    depends_on:
      gateway:
        condition: service_healthy
      benchmark-upstream:
        condition: service_healthy
    restart: "no"

  benchmark:
    build:
      <<: *app-build
      target: benchmark-runner
    profiles: [benchmark]
    command: ["-requests", "200", "-concurrency", "20"]
    depends_on:
      gateway:
        condition: service_healthy
      benchmark-upstream:
        condition: service_healthy
      benchmark-seed:
        condition: service_completed_successfully

volumes:
  demo-mysql:
  demo-redis:
  demo-gateway-data:
  demo-prometheus:
  demo-grafana:
